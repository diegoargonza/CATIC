---
import { string } from 'astro:schema'

 interface Props {

    titulo: string
    matiz: string
    imagen: string
    enlace?: string
    icon?: string
 }

 const {titulo, matiz, imagen, enlace, icon} = Astro.props;
---
 <a class="relative w-full shrink-0 md:h-full" href={enlace} target="_blank">
    <img src={imagen} class="w-full h-full" alt="siguiente referencia">
    <div class={`absolute inset-0 flex flex-col items-center justify-center text-white px-4 sm:px-10 md:px-20 lg:px-32 text-center ${matiz}`}>
        <h2 class="text-sm sm:text-base md:text-xl lg:text-3xl font-bold break-keep">{titulo}</h2>
        <i class={`${icon}`}></i>
    </div>
</a>



